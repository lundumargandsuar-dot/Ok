index.html
!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nexroa - Premium Website Templates</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        :root {
            --primary: #ff7a00;
            --primary-dark: #e56a00;
            --dark: #0b0f19;
            --darker: #05080f;
            --light: #ffffff;
            --gray: #8892b0;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            background: var(--dark);
            color: var(--light);
            overflow-x: hidden;
            position: relative;
        }

        .animated-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            background: linear-gradient(-45deg, #0b0f19, #1a1f2e, #162032, #0b0f19);
            background-size: 400% 400%;
            animation: gradientBG 15s ease infinite;
        }

        @keyframes gradientBG {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        nav {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            padding: 20px 50px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 1000;
            background: rgba(11, 15, 25, 0.95);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(255, 122, 0, 0.2);
        }

        .logo {
            font-size: 28px;
            font-weight: 900;
            color: var(--primary);
            text-decoration: none;
            letter-spacing: 2px;
        }

        .logo span {
            color: var(--light);
        }

        .nav-links {
            display: flex;
            gap: 30px;
            list-style: none;
        }

        .nav-links a {
            color: var(--light);
            text-decoration: none;
            font-weight: 500;
            transition: 0.3s;
            cursor: pointer;
        }

        .nav-links a:hover {
            color: var(--primary);
        }

        .hero {
            padding: 180px 20px 100px;
            text-align: center;
            position: relative;
            margin-top: 60px;
        }

        .hero h1 {
            font-size: 52px;
            font-weight: 900;
            opacity: 0;
            transform: translateY(50px);
            animation: fadeUp 1s 0.2s forwards;
            letter-spacing: -1px;
            line-height: 1.2;
        }

        .hero h1 span {
            color: var(--primary);
        }

        .hero p {
            opacity: 0;
            transform: translateY(50px);
            animation: fadeUp 1s 0.4s forwards;
            font-size: 18px;
            color: var(--gray);
            margin-top: 20px;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        @keyframes fadeUp {
            to { opacity: 1; transform: translateY(0); }
        }

        .stats {
            display: flex;
            justify-content: center;
            gap: 50px;
            flex-wrap: wrap;
            margin: 60px auto 0;
            max-width: 1000px;
            padding: 40px;
            background: rgba(255, 122, 0, 0.08);
            border-radius: 20px;
            border: 1px solid rgba(255, 122, 0, 0.2);
        }

        .stat-item {
            text-align: center;
            opacity: 0;
            animation: statFade 0.8s forwards;
        }

        @keyframes statFade {
            to { opacity: 1; }
        }

        .stat-item:nth-child(1) { animation-delay: 0.5s; }
        .stat-item:nth-child(2) { animation-delay: 0.7s; }
        .stat-item:nth-child(3) { animation-delay: 0.9s; }
        .stat-item:nth-child(4) { animation-delay: 1.1s; }

        .stat-number {
            font-size: 48px;
            font-weight: 900;
            color: var(--primary);
            text-shadow: 0 0 30px rgba(255, 122, 0, 0.5);
        }

        .stat-label {
            color: var(--gray);
            font-size: 14px;
            margin-top: 8px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .order-section {
            padding: 100px 20px;
            background: rgba(0, 0, 0, 0.3);
            margin-top: 50px;
        }

        .section-title {
            text-align: center;
            font-size: 42px;
            font-weight: 800;
            margin-bottom: 20px;
        }

        .section-title span {
            color: var(--primary);
        }

        .section-subtitle {
            text-align: center;
            color: var(--gray);
            margin-bottom: 50px;
            font-size: 18px;
        }

        form {
            max-width: 700px;
            margin: auto;
            background: rgba(17, 24, 39, 0.8);
            padding: 40px;
            border-radius: 25px;
            border: 1px solid rgba(255, 122, 0, 0.2);
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
        }

        .form-group {
            margin-bottom: 25px;
        }

        .form-group label {
            display: block;
            color: var(--light);
            font-weight: 500;
            margin-bottom: 10px;
            font-size: 14px;
        }

        .form-group label .required {
            color: var(--primary);
        }

        input, textarea, select {
            width: 100%;
            padding: 16px 20px;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 12px;
            font-size: 15px;
            color: var(--light);
            transition: 0.3s;
            outline: none;
            font-family: 'Poppins', sans-serif;
        }

        input:focus, textarea:focus, select:focus {
            border-color: var(--primary);
            box-shadow: 0 0 20px rgba(255, 122, 0, 0.2);
        }

        input::placeholder, textarea::placeholder {
            color: var(--gray);
        }

        textarea {
            height: 140px;
            resize: vertical;
        }

        select {
            cursor: pointer;
        }

        select option {
            background: #111827;
            color: #fff;
        }

        form button {
            width: 100%;
            background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
            color: #000;
            padding: 18px 40px;
            border: none;
            border-radius: 50px;
            font-weight: 700;
            cursor: pointer;
            font-size: 17px;
            transition: all 0.3s ease;
            margin-top: 10px;
            box-shadow: 0 10px 30px rgba(255, 122, 0, 0.3);
        }

        form button:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 40px rgba(255, 122, 0, 0.4);
        }

        form button:disabled {
            opacity: 0.6;
            cursor: not-allowed;
            transform: none;
        }

        .testimonials-section {
            padding: 100px 20px;
        }

        .testimonial-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            max-width: 1200px;
            margin: auto;
        }

        .testimonial {
            opacity: 0;
            transform: translateY(50px);
            animation: testimonialFade 0.6s ease forwards;
            background: rgba(17, 24, 39, 0.8);
            padding: 30px;
            border-radius: 20px;
            border: 1px solid rgba(255, 255, 255, 0.05);
            position: relative;
        }

        @keyframes testimonialFade {
            to { opacity: 1; transform: translateY(0); }
        }

        .testimonial:nth-child(1) { animation-delay: 0.2s; }
        .testimonial:nth-child(2) { animation-delay: 0.4s; }
        .testimonial:nth-child(3) { animation-delay: 0.6s; }

        .testimonial-header {
            display: flex;
            gap: 15px;
            margin-bottom: 20px;
            align-items: center;
        }

        .testimonial-avatar {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            font-size: 20px;
            color: #000;
            flex-shrink: 0;
        }

        .testimonial-info h4 {
            color: var(--light);
            font-size: 16px;
            margin: 0;
        }

        .testimonial-info .role {
            color: var(--gray);
            font-size: 13px;
            margin: 0;
        }

        .testimonial .stars {
            color: #ffd700;
            font-size: 16px;
            margin-bottom: 15px;
        }

        .testimonial p {
            color: var(--gray);
            font-size: 15px;
            line-height: 1.8;
            margin: 0;
            font-style: italic;
        }

        .thankyou {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.95);
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            opacity: 0;
            visibility: hidden;
            transition: 0.5s;
            transform: scale(0.8);
            z-index: 10000;
            padding: 20px;
            backdrop-filter: blur(5px);
        }

        .thankyou.active {
            opacity: 1;
            visibility: visible;
            transform: scale(1);
        }

        .thankyou-content {
            background: linear-gradient(135deg, #111827 0%, #1a1f2e 100%);
            border-radius: 30px;
            padding: 50px 40px;
            max-width: 500px;
            width: 100%;
            text-align: center;
            border: 2px solid rgba(255, 122, 0, 0.5);
            box-shadow: 0 0 50px rgba(255, 122, 0, 0.3), 0 30px 80px rgba(0, 0, 0, 0.5);
            animation: popupSlideIn 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
        }

        @keyframes popupSlideIn {
            0% {
                opacity: 0;
                transform: scale(0.3) translateY(50px);
            }
            50% {
                transform: scale(1.05);
            }
            100% {
                opacity: 1;
                transform: scale(1) translateY(0);
            }
        }

        .tick {
            font-size: 80px;
            margin-bottom: 20px;
            animation: tickAnimation 0.8s cubic-bezier(0.34, 1.56, 0.64, 1);
        }

        @keyframes tickAnimation {
            0% {
                transform: scale(0) rotateZ(-45deg);
                opacity: 0;
            }
            50% {
                transform: scale(1.2) rotateZ(10deg);
            }
            100% {
                transform: scale(1) rotateZ(0);
                opacity: 1;
            }
        }

        .thankyou h2 {
            font-size: 32px;
            margin-bottom: 15px;
            color: var(--light);
            animation: textFadeIn 0.8s ease 0.3s forwards;
            opacity: 0;
        }

        .thankyou p {
            color: var(--gray);
            font-size: 16px;
            line-height: 1.8;
            margin-bottom: 10px;
            animation: textFadeIn 0.8s ease 0.5s forwards;
            opacity: 0;
        }

        .thankyou .highlight {
            color: var(--primary);
            font-weight: 600;
            font-size: 18px;
            margin-top: 20px;
            display: block;
            animation: textFadeIn 0.8s ease 0.7s forwards;
            opacity: 0;
        }

        @keyframes textFadeIn {
            to {
                opacity: 1;
                transform: translateY(0);
            }
            from {
                opacity: 0;
                transform: translateY(10px);
            }
        }

        .thankyou button {
            margin-top: 30px;
            padding: 14px 40px;
            border: none;
            border-radius: 25px;
            background: var(--primary);
            color: #000;
            font-weight: 700;
            cursor: pointer;
            transition: 0.3s;
            font-size: 16px;
            animation: buttonPop 0.8s ease 0.9s forwards;
            opacity: 0;
            transform: scale(0.8);
        }

        @keyframes buttonPop {
            to {
                opacity: 1;
                transform: scale(1);
            }
        }

        .thankyou button:hover {
            background: var(--primary-dark);
            transform: translateY(-2px);
        }

        .error-message {
            color: #ff6b6b;
            padding: 10px;
            background: rgba(255, 107, 107, 0.1);
            border-radius: 8px;
            margin-bottom: 20px;
            display: none;
            text-align: center;
        }

        .error-message.show {
            display: block;
        }

        footer {
            background: #05080f;
            padding: 60px 20px 30px;
            text-align: center;
            border-top: 1px solid rgba(255, 122, 0, 0.2);
            margin-top: 50px;
        }

        footer h3 {
            color: var(--primary);
            font-size: 28px;
            margin-bottom: 10px;
        }

        footer p {
            color: var(--gray);
            margin-bottom: 30px;
        }

        .footer-links {
            display: flex;
            gap: 25px;
            justify-content: center;
            flex-wrap: wrap;
            margin: 30px 0;
        }

        .footer-links a {
            text-decoration: none;
            color: var(--light);
            font-size: 15px;
            display: flex;
            align-items: center;
            gap: 8px;
            transition: all 0.3s;
            padding: 12px 25px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 30px;
            cursor: pointer;
        }

        .footer-links a:hover {
            background: rgba(255, 122, 0, 0.2);
            color: var(--primary);
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(255, 122, 0, 0.2);
        }

        .footer-links a span {
            font-size: 20px;
        }

        .copyright {
            color: var(--gray);
            font-size: 14px;
            margin-top: 40px;
            padding-top: 30px;
            border-top: 1px solid rgba(255, 255, 255, 0.05);
        }

        @media(max-width: 768px) {
            nav {
                padding: 15px 20px;
            }

            .nav-links {
                display: none;
            }

            .hero {
                padding: 150px 20px 60px;
            }

            .hero h1 {
                font-size: 34px;
            }

            .hero p {
                font-size: 15px;
            }

            .section-title {
                font-size: 32px;
            }

            form {
                padding: 25px;
            }

            .stats {
                gap: 25px;
                padding: 25px;
            }

            .stat-number {
                font-size: 36px;
            }

            .thankyou-content {
                padding: 30px 20px;
            }

            .footer-links {
                flex-direction: column;
                align-items: center;
            }
        }
    </style>
</head>

<body>
    <div class="animated-bg"></div>

    <nav>
        <a href="#" class="logo">NEX<span>ROA</span></a>
        <ul class="nav-links">
            <li><a onclick="document.getElementById('order').scrollIntoView({behavior: 'smooth'})">Order Now</a></li>
            <li><a onclick="document.getElementById('testimonials').scrollIntoView({behavior: 'smooth'})">Reviews</a></li>
            <li><a onclick="document.getElementById('contact').scrollIntoView({behavior: 'smooth'})">Contact</a></li>
        </ul>
    </nav>

    <section class="hero">
        <h1>Premium Website <span>Templates</span><br>At Affordable Prices</h1>
        <p>Get a professional website for your business. Fill out the form below to get started.</p>

        <div class="stats">
            <div class="stat-item">
                <div class="stat-number">20+</div>
                <div class="stat-label">Templates</div>
            </div>
            <div class="stat-item">
                <div class="stat-number">200+</div>
                <div class="stat-label">Happy Clients</div>
            </div>
            <div class="stat-item">
                <div class="stat-number">48h</div>
                <div class="stat-label">Delivery Time</div>
            </div>
            <div class="stat-item">
                <div class="stat-number">✓</div>
                <div class="stat-label">Get Started</div>
            </div>
        </div>
    </section>

    <section class="order-section" id="order">
        <h2 class="section-title">Get Your <span>Website</span></h2>
        <p class="section-subtitle">Fill in your details and we'll help you get the perfect website</p>

        <form id="orderForm">
            <div class="error-message" id="errorMessage"></div>

            <div class="form-group">
                <label>Full Name <span class="required">*</span></label>
                <input type="text" name="name" placeholder="Enter your full name" required>
            </div>

            <div class="form-group">
                <label>Email Address <span class="required">*</span></label>
                <input type="email" name="email" placeholder="your@email.com" required>
            </div>

            <div class="form-group">
                <label>Contact Number <span class="required">*</span></label>
                <input type="tel" name="number" placeholder="+91 98765 43210" required>
            </div>

            <div class="form-group">
                <label>Country <span class="required">*</span></label>
                <select name="country" required>
                    <option value="">Select your country</option>
                    <option value="India">🇮🇳 India</option>
                    <option value="USA">🇺🇸 USA</option>
                    <option value="UAE">🇦🇪 UAE</option>
                    <option value="UK">🇬🇧 UK</option>
                    <option value="Canada">🇨🇦 Canada</option>
                    <option value="Australia">🇦🇺 Australia</option>
                    <option value="Other">🌍 Other</option>
                </select>
            </div>

            <div class="form-group">
                <label>Website Type <span class="required">*</span></label>
                <select name="website_type" required>
                    <option value="">Select website type</option>
                    <option value="Business">Business Website</option>
                    <option value="E-commerce">E-commerce Store</option>
                    <option value="Portfolio">Portfolio/Creative</option>
                    <option value="Restaurant">Restaurant/Cafe</option>
                    <option value="Healthcare">Healthcare/Medical</option>
                    <option value="Real Estate">Real Estate</option>
                    <option value="Education">Education/Academy</option>
     